<section class="min-h-screen flex items-center justify-center px-4">
  <div class="flex flex-col items-center justify-center text-center">
    <div class="w-full">
      <h1 class="title-retro title-retro-shadow text-3xl sm:text-4xl md:text-6xl lg:text-9xl">
        PORTFOLIO
      </h1>
    </div>
    <div class="w-full">
      <h2 class="blink subtitle-retro text-sm sm:text-xl md:text-2xl mt-11">
        SCROLL DOWN TO START!
      </h2>
      <h2
        class="moveupdown subtitle-retro text-xl sm:text-2xl md:text-4xl mt-7"
      >
        v
      </h2>
    </div>
  </div>
</section>

<style>
  @import url("https://fonts.googleapis.com/css2?family=Press+Start+2P&display=swap");

  .title-retro {
    font-family: "Press Start 2P", serif;
    color: #ff9ef0;
  }

  .title-retro-shadow {
    text-shadow: 0.08em 0.08em #111fe4;
  }

  .subtitle-retro {
    font-family: "Press Start 2P", serif;
    color: #ff9ef0;
    background: linear-gradient(to top, #ff9ef0 0 35%, #ffffff);
    background-clip: text;
    color: transparent;
  }

  .blink {
    animation: blinker 1s infinite;
  }

  @keyframes blinker {
    0% {
      opacity: 0;
    }
    49% {
      opacity: 0;
    }
    50% {
      opacity: 1;
    }
  }

  .moveupdown {
    -webkit-animation: action 1s infinite alternate;
    animation: action 1s infinite alternate;
  }
  @-webkit-keyframes action {
    0% {
      transform: translateY(0);
    }
    100% {
      transform: translateY(-10px);
    }
  }

  @keyframes action {
    0% {
      transform: translateY(0);
    }
    100% {
      transform: translateY(-10px);
    }
  }
</style>
